class Solution:
    def addDigits(self, num: int) -> int:
        while 1 :
            sum=0
            for ch_digit in str(num):
                sum+=int(ch_digit)
            if sum <= 9 :
                return sum
            else :
                num = sum
