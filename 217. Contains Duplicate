class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        # bucket = set()
        # for num in nums :
        #     if num in bucket:
        #         return True
        #     else :
        #         bucket.add(num)
        # return False
        if len(nums) != len(set(nums)) :
            return True
        else :
            return False
