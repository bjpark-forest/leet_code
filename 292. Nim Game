class Solution:
    def canWinNim(self, n: int) -> bool:
        return not n%4==0
# 4의 배수가 되게 돌이 남으면 내가 진다.
# 왜냐하면 1개를 가져가도 , 2개를 가져가도, 3개를 가져가도 1~3개의 돌이 남아 상대방이 모두 가져갈 수 있기 때문이다.
# 5개가 남으면 1개를 가져가고
# 6개가 남으면 2개를 가져가고
# 7개가 남으면 3개를 가져가면 상대방이 진다.
# 마찬가지로 8개가 남으면 내가 진다.
# 즉 4의 배수가 되면 내가 진다.
# if n%4==0 :
#   return False
# return True
# 이것보다 return not n%4==0이 빠르다
