class Solution:
    def finalString(self, s: str) -> str:
        result = ''
        for each in s:
            if each != 'i':
                result += each
            else: 
                result = result[::-1]
        return result
