class Solution:
    def numJewelsInStones(self, jewels: str, stones: str) -> int:
        result = 0
        for x in stones :
            if x in jewels :
                result += 1
        return result
